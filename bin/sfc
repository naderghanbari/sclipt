#!/usr/bin/env scala

// Scala script which counts number of files.

import scala.language.postfixOps
import java.io.File

val files = args map (new File(_)) filter (_ exists) filter (_ isFile)
println(s"Total files: ${files.size}")

#!/usr/bin/env scala

// Scala script which counts the number of lines in a file.
def lineCount(fileName: String) = scala.io.Source.fromFile(fileName).getLines().size
args zip (args map lineCount) foreach println